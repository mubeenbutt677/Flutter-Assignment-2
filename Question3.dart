void main() {
  print(
      "Cost of one movie ticket is 600 PKR. Write a script to store ticket price in a \nvariable & calculate the cost of buying 5 tickets to a movie.");
  var ticketCost = 600;
  var noOfTicket = 5;

  print(ticketCost * noOfTicket);
}
